using System;
using System.Collections.Generic;
using System.Text;
using System.Net.Mail;

namespace HiLand.Utility.Net.Mail
{
    public static class MailHelper
    {
        //private static SmtpClient GetSmtpClient()
        //{
        //    SmtpClient client = new SmtpClient("");
        //    client.Port = siteSettings.SmtpServerPort;
        //    if (siteSettings.SmtpServerEnableSsl) client.EnableSsl = siteSettings.SmtpServerEnableSsl;
        //    if (siteSettings.SmtpServerRequiredLogin)
        //    {
        //        client.UseDefaultCredentials = false;
        //        client.Credentials = new NetworkCredential(siteSettings.SmtpServerUserName, siteSettings.SmtpServerPassword);
        //        client.DeliveryMethod = SmtpDeliveryMethod.Network;
        //    }
        //    return client;
        //}

    }
}
